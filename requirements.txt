# NOTE: This project uses pyproject.toml with uv for dependency management.
# For the best experience, use uv:
#   curl -LsSf https://astral.sh/uv/install.sh | sh
#   uv sync --all-extras
#
# This requirements.txt is provided for pip compatibility only.

# Runtime dependencies
pyyaml>=6.0

# Optional: Ray (for local testing)
# ray[default]>=2.9.0
# numpy>=1.20.0

# Optional: Development/testing
# pytest>=7.0
# pytest-cov>=4.0
